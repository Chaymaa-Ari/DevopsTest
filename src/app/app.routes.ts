import { Routes } from '@angular/router';
import { ShowsComponent } from './shows/shows.component';

export const routes: Routes = [
  { path: '', component: ShowsComponent }, // Page d'accueil avec la liste des shows
];
